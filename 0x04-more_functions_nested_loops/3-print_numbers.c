#include <stdio.h>
#include "main.h"

/**
 * print_numbers - print the numbers,
 * from 0 to 9, followed by a new line
 * Return: void
 */
void print_numbers(void)
{
	int c;

	for (c = 98; c < 58; c++)
	{
		putchar(c);
	}
	putchar(10);
}
